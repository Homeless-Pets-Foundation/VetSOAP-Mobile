name: Self-hosted CI

run-name: Self-hosted CI for ${{ github.ref_name }} at ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: self-hosted-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  confirm-pr-head:
    name: Confirm PR head
    runs-on: "runs-on=${{ github.run_id }}-confirm-pr-head-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 5
    outputs:
      base-sha: ${{ steps.confirm.outputs.base-sha }}
      head-sha: ${{ steps.confirm.outputs.head-sha }}
      pr-number: ${{ steps.confirm.outputs.pr-number }}
    steps:
      - name: Confirm one open same-repository PR at the dispatch SHA
        id: confirm
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: |
            if (process.env.GITHUB_REF_TYPE !== 'branch' || !context.ref.startsWith('refs/heads/')) {
              core.setFailed(`Dispatch must target a branch, not ${context.ref}.`);
              return;
            }

            const { owner, repo } = context.repo;
            const branch = context.ref.slice('refs/heads/'.length);
            const repository = `${owner}/${repo}`;
            const pulls = await github.paginate(github.rest.pulls.list, {
              owner,
              repo,
              state: 'open',
              head: `${owner}:${branch}`,
              per_page: 100,
            });
            const matching = pulls.filter((pull) =>
              pull.head.ref === branch && pull.head.repo?.full_name === repository
            );

            if (matching.length !== 1) {
              core.setFailed(
                `Expected exactly one open same-repository PR for ${branch}; found ${matching.length}.`,
              );
              return;
            }

            const pull = matching[0];
            const defaultBranch = context.payload.repository.default_branch;
            if (pull.base.ref !== defaultBranch) {
              core.setFailed(
                `PR #${pull.number} targets ${pull.base.ref}; expected ${defaultBranch}.`,
              );
              return;
            }
            if (pull.head.sha !== context.sha) {
              core.setFailed(
                `Dispatch SHA ${context.sha} is stale; PR #${pull.number} now points to ${pull.head.sha}.`,
              );
              return;
            }

            core.setOutput('base-sha', pull.base.sha);
            core.setOutput('head-sha', pull.head.sha);
            core.setOutput('pr-number', String(pull.number));
            core.summary
              .addHeading('Confirmed pull request head')
              .addTable([
                ['PR', 'Branch', 'Base SHA', 'Head SHA'],
                [`#${pull.number}`, branch, pull.base.sha, pull.head.sha],
              ]);
            await core.summary.write();

  r2-destination-contract:
    name: R2 Destination Contract
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-r2-destination-contract-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 15
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Verify R2 contracts
        run: ./scripts/ci.sh r2

  typecheck:
    name: Typecheck
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-typecheck-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 15
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: TypeScript
        run: ./scripts/ci.sh typecheck

  lint:
    name: Lint
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-lint-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 15
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: ESLint
        run: ./scripts/ci.sh lint

  test:
    name: Test
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-test-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 30
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: ./scripts/ci.sh test

  analyze:
    name: Analyze (${{ matrix.language }})
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-analyze-${{ strategy.job-index }}-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript]
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
        with:
          languages: ${{ matrix.language }}
          trap-caching: false
          dependency-caching: false
      - name: Autobuild
        uses: github/codeql-action/autobuild@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
        with:
          category: /language:${{ matrix.language }}
          upload: always
          upload-database: false

  dependency-review:
    name: Review
    needs: confirm-pr-head
    runs-on: "runs-on=${{ github.run_id }}-dependency-review-${{ github.run_attempt }}/runner=linux-ci"
    timeout-minutes: 10
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Dependency Review
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4
        with:
          base-ref: ${{ needs.confirm-pr-head.outputs.base-sha }}
          head-ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          fail-on-severity: high

  swift-typecheck:
    name: Swift typecheck (durable recorder)
    needs: confirm-pr-head
    runs-on: [self-hosted, local-ci, macOS, ARM64]
    timeout-minutes: 10
    steps:
      - name: Checkout confirmed PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.confirm-pr-head.outputs.head-sha }}
          clean: true
          persist-credentials: false
      - name: Report and verify commit
        env:
          CONFIRMED_SHA: ${{ needs.confirm-pr-head.outputs.head-sha }}
        run: |
          set -euo pipefail
          checked_out_sha="$(git rev-parse HEAD)"
          echo "Runner: $RUNNER_NAME ($RUNNER_OS/$RUNNER_ARCH)"
          echo "GITHUB_SHA=$GITHUB_SHA"
          echo "git rev-parse HEAD=$checked_out_sha"
          test "$GITHUB_SHA" = "$CONFIRMED_SHA"
          test "$checked_out_sha" = "$CONFIRMED_SHA"
      - name: Toolchain
        run: |
          xcodebuild -version
          xcrun swiftc --version
      - name: Type-check ExpoModulesCore-free Swift
        run: ./scripts/ci.sh swift
